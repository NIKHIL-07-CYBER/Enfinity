{
  "name": "enfinity",
  "private": true,
  "workspaces": [
    "UI/adaptive-reader",
    "telemetry",
    "backend"
  ],
  "scripts": {
    "start": "cd backend && node dist/server/index.js",
    "build:backend": "cd backend && npm run build",
    "dev": "cd backend && npm run dev"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3"
  }
}
